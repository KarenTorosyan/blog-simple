import { DateOfEntityPipe } from './date-of-entity.pipe';

describe('DateOfEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
